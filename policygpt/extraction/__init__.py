"""policygpt.extraction — document extraction and text processing.

Modules:
    taxonomy         — stopwords, keywords, intent patterns, text helpers
    redaction        — PII masking/unmasking via Redactor
    file_extractor   — HTML/PDF/plain-text → (title, sections) extraction
    metadata_extractor — document and section metadata extraction
    entity_extractor — LLM-based contextual entity extraction
"""
